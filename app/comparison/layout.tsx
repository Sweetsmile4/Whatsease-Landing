import { ReactNode } from "react";
import InternalChrome from "../components/InternalChrome";

export default function ComparisonLayout({ children }: { children: ReactNode }) { return <InternalChrome>{children}</InternalChrome>; }
